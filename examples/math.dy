mod basic {
    let add(v1: int, v2: int) -> int {
        return v1 + v2;
    }

    let sub(v1: int, v2: int) -> int {
        return v1 - v2;
    }

    let mul(v1: int, v2: int) -> int {
        return v1 * v2;
    }

    let div(v1: int, v2: int) -> int {
        return v1 / v2;
    }
}
