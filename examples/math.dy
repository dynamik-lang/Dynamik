mod basic {
    fn add(v1: int, v2: int) -> int {
        return v1 + v2;
    }

    fn sub(v1: int, v2: int) -> int {
        return v1 - v2;
    }

    fn mul(v1: int, v2: int) -> int {
        return v1 * v2;
    }

    fn div(v1: int, v2: int) -> int {
        return v1 / v2;
    }
}
