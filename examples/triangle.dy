extern "C" let printf(string, ...) -> int;

let height: int = 5;
let num: int = 1;
let i: int = 1;
let j: int = 0;

while i - 1 < height {
    while j < i {
        printf("%lld ", num);

        j = j + 1;
        num = num + 1;
    }

    printf("\n");

    j = 0;
    num = 1;
    i = i + 1;
}
